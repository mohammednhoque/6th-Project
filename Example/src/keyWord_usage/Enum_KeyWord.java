package keyWord_usage;

public class Enum_KeyWord {

	public static void main(String[] args) {
		//what is enum?
		// An enum is a special "class" that represents
		//a group of constants (unchangeable variables,
		//like final variables). To create an enum , use
		//the enum keyword (instead of class or interface), 
		//and separate the constants with a comma.

		//enum myenum { A, B, C, D };
	
		
		
		
		
		
	}

}
